export interface RaceData {
  flag_state: string | number;
  run_name: string;
  series_id: string | number;
  track_name: string;
  lap_number: string;
  laps_in_race: string;
  time_of_day_os: string | number | Date;
}
